name: Publish Pull Request
on:
  pull_request:
    branches:
      - develop

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Setup Node.js
        uses: actions/setup-node@v2
        with:
          node-version: '20.15.0' # Use Node.js >=18.0.0

      - name: Install Expo CLI and Dependencies
        run: |
          npm install -g expo-cli eas-cli
          npm install react-native@0.76.5

      - name: Install Project Dependencies
        run: |
          rm -rf node_modules
          npm cache clean --force
          npm install

      - name: Install React Native Dependencies for Expo
        run: expo install react-native

      - name: Debug Installed Packages
        run: npm list react-native

      - name: Publish to Expo
        run: NODE_ENV=dev expo publish --non-interactive --release-channel pr-${{ github.event.number }}

      - name: Add Expo Link to PR
        uses: actions/github-script@v6
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          script: |
            const prNumber = context.payload.pull_request.number;
            const releaseChannel = `pr-${prNumber}`;
            const expoUrl = `https://expo.dev/@your-username/your-project?release-channel=${releaseChannel}`;
            const qrCodeUrl = `https://api.qrserver.com/v1/create-qr-code/?data=${encodeURIComponent(expoUrl)}&size=200x200`;

            github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber,
              body: `## Test this PR with Expo Go\n\nYour build has been published to Expo! 🎉\n\nScan the QR code below or click the link to open the app in Expo Go:\n\n🔗 [Open in Expo](${expoUrl})\n\n![Expo QR Code](${qrCodeUrl})`,
            });
